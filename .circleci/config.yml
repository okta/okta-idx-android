version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0.3
    steps:
      - checkout
      - run:
          name: Build
          command: ./gradlew assembleDebug testDebugUnitTest assembleAndroidTest

workflows:
  build_and_test:
    jobs:
      - build
